# API de cobro — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Cobrar un pedido creando su venta 1:1 con desglose de IVA, pagos (pago dividido) y cambio, generando ticket y liberando la mesa; más el listado de pendientes de cobro.

**Architecture:** Un `venta_service` concentra la lógica (validación por rol, desglose de IVA, cobro transaccional, armado de la respuesta). Dos endpoints nuevos (`POST /ventas`, `GET /ventas/{id}`) y un filtro `?por_cobrar=true` en el listado de pedidos. Sin migración: el esquema (`ventas`/`pagos`/`tickets`/`metodos_pago`/`configuracion`) ya existe.

**Tech Stack:** FastAPI · SQLAlchemy · Pydantic v2 · PostgreSQL · pytest (en el contenedor Docker).

## Global Constraints

- Backend tests: `docker compose exec -T api pytest ...` (requiere `docker compose up -d`).
- Sin migración Alembic: tablas y catálogos ya existen; el IVA se añade como fila de `configuracion` vía el seed (idempotente).
- Seguir patrones: services lanzan `HTTPException(status.<CODE>, "mensaje")`; endpoints usan `Depends(get_current_user)`; el rol se lee de `usuario.rol.nombre_rol` dentro del service.
- **IVA incluido/desglosado:** `Venta.total = total del pedido`; `base = round(total/(1+tasa), 2)`, `iva = total − base`. Tasa por defecto `Decimal("0.16")`.
- **Cobrar** solo **Cajero / Administrador**. **Folio:** `f"V-{id_venta:06d}"` (p. ej. `V-000123`).
- Estados de pedido (nombre exacto): `Cancelado` es el no-cobrable.

---

### Task 1: Fundamentos — `desglose`, tasa de IVA y seed

**Files:**
- Create: `backend/app/services/venta_service.py`
- Modify: `backend/app/db/seed.py` (fila `configuracion.iva_tasa`)
- Test: `backend/tests/test_venta_service.py` (crear)

**Interfaces:**
- Consumes: `Configuracion` de `app.models`.
- Produces:
  - `venta_service.desglose(total: Decimal, tasa: Decimal) -> tuple[Decimal, Decimal]` (base, iva)
  - `venta_service._iva_tasa(db: Session) -> Decimal`

- [ ] **Step 1: Escribir el test de `desglose` (falla)**

Crear `backend/tests/test_venta_service.py`:

```python
from decimal import Decimal

from app.services.venta_service import desglose


def test_desglose_iva_incluido():
    base, iva = desglose(Decimal("116"), Decimal("0.16"))
    assert base == Decimal("100.00")
    assert iva == Decimal("16.00")


def test_desglose_redondea_a_centavos():
    base, iva = desglose(Decimal("100"), Decimal("0.16"))
    assert base == Decimal("86.21")
    assert iva == Decimal("13.79")
```

- [ ] **Step 2: Ejecutar para verificar que falla**

Run: `docker compose exec -T api pytest tests/test_venta_service.py -v`
Expected: FAIL (no existe `app.services.venta_service`).

- [ ] **Step 3: Crear `venta_service.py` con `desglose` y `_iva_tasa`**

Crear `backend/app/services/venta_service.py`:

```python
from decimal import ROUND_HALF_UP, Decimal

from sqlalchemy import select
from sqlalchemy.orm import Session

from app.models import Configuracion

_IVA_DEFAULT = Decimal("0.16")


def _iva_tasa(db: Session) -> Decimal:
    row = db.execute(
        select(Configuracion).where(Configuracion.clave == "iva_tasa")
    ).scalar_one_or_none()
    return Decimal(row.valor) if row else _IVA_DEFAULT


def desglose(total: Decimal, tasa: Decimal) -> tuple[Decimal, Decimal]:
    base = (total / (Decimal("1") + tasa)).quantize(
        Decimal("0.01"), rounding=ROUND_HALF_UP
    )
    iva = total - base
    return base, iva
```

- [ ] **Step 4: Ejecutar el test (pasa)**

Run: `docker compose exec -T api pytest tests/test_venta_service.py -v`
Expected: PASS (2 tests).

- [ ] **Step 5: Sembrar `iva_tasa` en el seed**

En `backend/app/db/seed.py`, añadir `Configuracion` al import de modelos:

```python
from app.models import (
    Categoria,
    CategoriaGasto,
    Configuracion,
    EstadoPedido,
    Mesa,
    MetodoPago,
    Producto,
    Rol,
    UnidadMedida,
    Usuario,
)
```

Y añadir una entrada al final de la lista `SEED` (tras el bloque de `CategoriaGasto`):

```python
    (
        Configuracion,
        "clave",
        [
            {
                "clave": "iva_tasa",
                "valor": "0.16",
                "descripcion": "Tasa de IVA (fracción)",
            },
        ],
    ),
```

- [ ] **Step 6: Ejecutar el seed y la suite (sin regresiones)**

Run: `docker compose exec -T api python -m app.db.seed && docker compose exec -T api pytest -q`
Expected: seed idempotente OK; suite en verde (86 previos + 2 nuevos = 88).

- [ ] **Step 7: Commit**

```bash
git add backend/app/services/venta_service.py backend/app/db/seed.py \
  backend/tests/test_venta_service.py
git commit -m "feat(api): desglose de IVA y seed de iva_tasa

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 2: Cobro — `POST /ventas` y `GET /ventas/{id}`

**Files:**
- Modify: `backend/app/models/venta.py` (relaciones `Venta.pagos`, `Venta.ticket`, `Pago.metodo`)
- Create: `backend/app/schemas/venta.py`
- Modify: `backend/app/services/venta_service.py` (`cobrar`, `get_or_404`, `to_out`)
- Create: `backend/app/api/v1/ventas.py`
- Modify: `backend/app/api/v1/router.py`
- Modify: `backend/tests/conftest.py` (fixtures `cajero`, `cajero_headers`)
- Test: `backend/tests/test_ventas_api.py` (crear)

**Interfaces:**
- Consumes: `desglose`, `_iva_tasa` (Task 1); `Pedido`, `Venta`, `Pago`, `Ticket`,
  `MetodoPago` de `app.models`; `get_current_user`; endpoints de mesas/productos/pedidos
  y `POST /pedidos/{id}/cancelar` (slice 1 del Sprint 3).
- Produces:
  - `venta_service.cobrar(db: Session, data: VentaCreate, usuario) -> Venta`
  - `venta_service.get_or_404(db: Session, id_venta: int) -> Venta`
  - `venta_service.to_out(db: Session, venta: Venta) -> VentaOut`
  - Schemas `PagoIn`, `VentaCreate`, `MetodoResumen`, `PagoOut`, `VentaOut`
  - `POST /api/v1/ventas` → 201 `VentaOut`; `GET /api/v1/ventas/{id}` → `VentaOut`

- [ ] **Step 1: Añadir fixtures de cajero al conftest**

En `backend/tests/conftest.py`, tras `cocinero_headers`, añadir:

```python
@pytest.fixture()
def cajero(db):
    return _crear_usuario(db, "cajerotest", "cajero.test@cafeteria.com", "Cajero")


@pytest.fixture()
def cajero_headers(client, cajero):
    return _headers(client, "cajero.test@cafeteria.com")
```

- [ ] **Step 2: Escribir los tests de cobro (fallan)**

Crear `backend/tests/test_ventas_api.py`:

```python
def _pedido(client, db, admin_headers, numero, precio=116.0):
    from app.models import Categoria

    mesa = client.post(
        "/api/v1/mesas",
        headers=admin_headers,
        json={"numero_mesa": numero, "capacidad": 4},
    ).json()
    cat = db.query(Categoria).first()
    prod = client.post(
        "/api/v1/productos",
        headers=admin_headers,
        json={
            "id_categoria": cat.id_categoria,
            "nombre_producto": "Item",
            "precio_venta": precio,
            "disponible": True,
        },
    ).json()
    return client.post(
        "/api/v1/pedidos",
        headers=admin_headers,
        json={
            "id_mesa": mesa["id_mesa"],
            "items": [{"id_producto": prod["id_producto"], "cantidad": 1}],
        },
    ).json()


def _metodo_id(db, nombre):
    from app.models import MetodoPago

    return (
        db.query(MetodoPago)
        .filter(MetodoPago.nombre_metodo == nombre)
        .one()
        .id_metodo_pago
    )


def test_cobrar_ok(client, db, admin_headers, cajero_headers):
    pedido = _pedido(client, db, admin_headers, numero=601, precio=116.0)
    efectivo = _metodo_id(db, "Efectivo")
    r = client.post(
        "/api/v1/ventas",
        headers=cajero_headers,
        json={
            "id_pedido": pedido["id_pedido"],
            "pagos": [{"id_metodo_pago": efectivo, "monto": 200.0}],
        },
    )
    assert r.status_code == 201
    body = r.json()
    assert float(body["total"]) == 116.0
    assert float(body["subtotal"]) == 100.0
    assert float(body["iva"]) == 16.0
    assert float(body["cambio"]) == 84.0
    assert body["folio"].startswith("V-")
    m = client.get(
        f"/api/v1/mesas/{pedido['id_mesa']}", headers=admin_headers
    ).json()
    assert m["estado"] == "Disponible"


def test_cobrar_pago_dividido_exacto(client, db, admin_headers, cajero_headers):
    pedido = _pedido(client, db, admin_headers, numero=602, precio=116.0)
    ef = _metodo_id(db, "Efectivo")
    ta = _metodo_id(db, "Tarjeta")
    r = client.post(
        "/api/v1/ventas",
        headers=cajero_headers,
        json={
            "id_pedido": pedido["id_pedido"],
            "pagos": [
                {"id_metodo_pago": ef, "monto": 100.0},
                {"id_metodo_pago": ta, "monto": 16.0},
            ],
        },
    )
    assert r.status_code == 201
    body = r.json()
    assert float(body["cambio"]) == 0.0
    assert len(body["pagos"]) == 2


def test_cobrar_pago_insuficiente_422(client, db, admin_headers, cajero_headers):
    pedido = _pedido(client, db, admin_headers, numero=603, precio=116.0)
    ef = _metodo_id(db, "Efectivo")
    r = client.post(
        "/api/v1/ventas",
        headers=cajero_headers,
        json={
            "id_pedido": pedido["id_pedido"],
            "pagos": [{"id_metodo_pago": ef, "monto": 50.0}],
        },
    )
    assert r.status_code == 422


def test_cobrar_metodo_inexistente_422(client, db, admin_headers, cajero_headers):
    pedido = _pedido(client, db, admin_headers, numero=604, precio=116.0)
    r = client.post(
        "/api/v1/ventas",
        headers=cajero_headers,
        json={
            "id_pedido": pedido["id_pedido"],
            "pagos": [{"id_metodo_pago": 99999, "monto": 200.0}],
        },
    )
    assert r.status_code == 422


def test_cobrar_pagos_vacios_422(client, db, admin_headers, cajero_headers):
    pedido = _pedido(client, db, admin_headers, numero=605)
    r = client.post(
        "/api/v1/ventas",
        headers=cajero_headers,
        json={"id_pedido": pedido["id_pedido"], "pagos": []},
    )
    assert r.status_code == 422


def test_cobrar_pedido_cancelado_409(client, db, admin_headers, cajero_headers):
    pedido = _pedido(client, db, admin_headers, numero=606)
    client.post(
        f"/api/v1/pedidos/{pedido['id_pedido']}/cancelar",
        headers=admin_headers,
        json={"motivo": "prueba"},
    )
    ef = _metodo_id(db, "Efectivo")
    r = client.post(
        "/api/v1/ventas",
        headers=cajero_headers,
        json={
            "id_pedido": pedido["id_pedido"],
            "pagos": [{"id_metodo_pago": ef, "monto": 200.0}],
        },
    )
    assert r.status_code == 409


def test_cobrar_dos_veces_409(client, db, admin_headers, cajero_headers):
    pedido = _pedido(client, db, admin_headers, numero=607, precio=116.0)
    ef = _metodo_id(db, "Efectivo")
    payload = {
        "id_pedido": pedido["id_pedido"],
        "pagos": [{"id_metodo_pago": ef, "monto": 200.0}],
    }
    assert (
        client.post("/api/v1/ventas", headers=cajero_headers, json=payload).status_code
        == 201
    )
    assert (
        client.post("/api/v1/ventas", headers=cajero_headers, json=payload).status_code
        == 409
    )


def test_cobrar_rol_mesero_403(client, db, admin_headers, mesero_headers):
    pedido = _pedido(client, db, admin_headers, numero=608, precio=116.0)
    r = client.post(
        "/api/v1/ventas",
        headers=mesero_headers,
        json={
            "id_pedido": pedido["id_pedido"],
            "pagos": [{"id_metodo_pago": 1, "monto": 200.0}],
        },
    )
    assert r.status_code == 403


def test_get_venta_detalle_y_404(client, db, admin_headers, cajero_headers):
    pedido = _pedido(client, db, admin_headers, numero=609, precio=116.0)
    ef = _metodo_id(db, "Efectivo")
    venta = client.post(
        "/api/v1/ventas",
        headers=cajero_headers,
        json={
            "id_pedido": pedido["id_pedido"],
            "pagos": [{"id_metodo_pago": ef, "monto": 116.0}],
        },
    ).json()
    r = client.get(f"/api/v1/ventas/{venta['id_venta']}", headers=cajero_headers)
    assert r.status_code == 200
    assert float(r.json()["iva"]) == 16.0
    assert len(r.json()["pagos"]) == 1
    assert client.get("/api/v1/ventas/999999", headers=cajero_headers).status_code == 404
```

- [ ] **Step 3: Ejecutar para verificar que fallan**

Run: `docker compose exec -T api pytest tests/test_ventas_api.py -v`
Expected: FAIL (`/ventas` da 404 — no existe la ruta).

- [ ] **Step 4: Añadir relaciones ORM en `models/venta.py`**

En `backend/app/models/venta.py`, añadir el import de `relationship`:

```python
from sqlalchemy.orm import relationship
```

Dentro de la clase `Venta`, tras `estado_venta`, añadir:

```python
    pagos = relationship("Pago", lazy="selectin")
    ticket = relationship("Ticket", uselist=False, lazy="joined")
```

Dentro de la clase `Pago`, tras `fecha_pago`, añadir:

```python
    metodo = relationship("MetodoPago", lazy="joined")
```

- [ ] **Step 5: Crear los schemas en `schemas/venta.py`**

Crear `backend/app/schemas/venta.py`:

```python
from datetime import datetime
from decimal import Decimal

from pydantic import BaseModel, ConfigDict, Field


class PagoIn(BaseModel):
    id_metodo_pago: int
    monto: Decimal = Field(gt=0)
    referencia: str | None = None


class VentaCreate(BaseModel):
    id_pedido: int
    pagos: list[PagoIn] = Field(min_length=1)


class MetodoResumen(BaseModel):
    model_config = ConfigDict(from_attributes=True)

    id_metodo_pago: int
    nombre_metodo: str


class PagoOut(BaseModel):
    model_config = ConfigDict(from_attributes=True)

    id_pago: int
    id_metodo_pago: int
    metodo: MetodoResumen
    monto: Decimal
    referencia: str | None


class VentaOut(BaseModel):
    model_config = ConfigDict(from_attributes=True)

    id_venta: int
    id_pedido: int
    fecha_venta: datetime
    estado_venta: str
    folio: str
    total: Decimal
    subtotal: Decimal
    iva: Decimal
    cambio: Decimal
    pagos: list[PagoOut]
```

- [ ] **Step 6: Añadir `cobrar`, `get_or_404` y `to_out` al service**

En `backend/app/services/venta_service.py`, actualizar imports y añadir la lógica.

Reemplazar la cabecera de imports por:

```python
from decimal import ROUND_HALF_UP, Decimal

from fastapi import HTTPException, status
from sqlalchemy import select
from sqlalchemy.orm import Session

from app.models import Configuracion, MetodoPago, Pago, Pedido, Ticket, Venta
from app.schemas.venta import PagoOut, VentaCreate, VentaOut

_ROLES_COBRO = {"Cajero", "Administrador"}
```

(Se conservan `_IVA_DEFAULT`, `_iva_tasa` y `desglose` ya presentes.)

Añadir al final del archivo:

```python
def get_or_404(db: Session, id_venta: int) -> Venta:
    obj = db.get(Venta, id_venta)
    if obj is None:
        raise HTTPException(status.HTTP_404_NOT_FOUND, "Venta no encontrada")
    return obj


def cobrar(db: Session, data: VentaCreate, usuario) -> Venta:
    if usuario.rol.nombre_rol not in _ROLES_COBRO:
        raise HTTPException(status.HTTP_403_FORBIDDEN, "Rol no autorizado para cobrar")

    pedido = db.get(Pedido, data.id_pedido)
    if pedido is None:
        raise HTTPException(status.HTTP_404_NOT_FOUND, "Pedido no encontrado")
    if pedido.estado.nombre_estado == "Cancelado":
        raise HTTPException(
            status.HTTP_409_CONFLICT, "No se puede cobrar un pedido cancelado"
        )
    ya = db.execute(
        select(Venta).where(Venta.id_pedido == pedido.id_pedido)
    ).scalar_one_or_none()
    if ya is not None:
        raise HTTPException(status.HTTP_409_CONFLICT, "El pedido ya fue cobrado")

    for p in data.pagos:
        if db.get(MetodoPago, p.id_metodo_pago) is None:
            raise HTTPException(
                status.HTTP_422_UNPROCESSABLE_ENTITY,
                f"Método de pago {p.id_metodo_pago} inexistente",
            )

    total = pedido.total
    suma = sum((p.monto for p in data.pagos), Decimal("0"))
    if suma < total:
        raise HTTPException(status.HTTP_422_UNPROCESSABLE_ENTITY, "Pago insuficiente")

    venta = Venta(
        id_pedido=pedido.id_pedido, id_usuario=usuario.id_usuario, total=total
    )
    db.add(venta)
    db.flush()
    for p in data.pagos:
        db.add(
            Pago(
                id_venta=venta.id_venta,
                id_metodo_pago=p.id_metodo_pago,
                monto=p.monto,
                referencia=p.referencia,
            )
        )
    db.add(Ticket(id_venta=venta.id_venta, folio=f"V-{venta.id_venta:06d}"))
    pedido.mesa.estado = "Disponible"
    db.commit()
    db.refresh(venta)
    return venta


def to_out(db: Session, venta: Venta) -> VentaOut:
    base, iva = desglose(venta.total, _iva_tasa(db))
    suma = sum((p.monto for p in venta.pagos), Decimal("0"))
    return VentaOut(
        id_venta=venta.id_venta,
        id_pedido=venta.id_pedido,
        fecha_venta=venta.fecha_venta,
        estado_venta=venta.estado_venta,
        folio=venta.ticket.folio,
        total=venta.total,
        subtotal=base,
        iva=iva,
        cambio=suma - venta.total,
        pagos=[PagoOut.model_validate(p) for p in venta.pagos],
    )
```

- [ ] **Step 7: Crear el router `ventas.py`**

Crear `backend/app/api/v1/ventas.py`:

```python
from fastapi import APIRouter, Depends, status
from sqlalchemy.orm import Session

from app.core import deps
from app.db.session import get_db
from app.models import Usuario
from app.schemas.venta import VentaCreate, VentaOut
from app.services import venta_service

router = APIRouter(prefix="/ventas", tags=["ventas"])


@router.post("", response_model=VentaOut, status_code=status.HTTP_201_CREATED)
def cobrar(
    data: VentaCreate,
    db: Session = Depends(get_db),
    current: Usuario = Depends(deps.get_current_user),
):
    venta = venta_service.cobrar(db, data, current)
    return venta_service.to_out(db, venta)


@router.get("/{id_venta}", response_model=VentaOut)
def detalle(
    id_venta: int,
    db: Session = Depends(get_db),
    current: Usuario = Depends(deps.get_current_user),
):
    return venta_service.to_out(db, venta_service.get_or_404(db, id_venta))
```

- [ ] **Step 8: Registrar el router**

En `backend/app/api/v1/router.py`, añadir `ventas` al import y al include:

```python
from app.api.v1 import (
    auth,
    categorias,
    estados,
    mesas,
    pedidos,
    productos,
    roles,
    usuarios,
    ventas,
)
```

Tras `api_router.include_router(estados.router)`:

```python
api_router.include_router(ventas.router)
```

- [ ] **Step 9: Ejecutar los tests de ventas (pasan)**

Run: `docker compose exec -T api pytest tests/test_ventas_api.py -v`
Expected: PASS (9 tests).

- [ ] **Step 10: Suite backend completa (sin regresiones)**

Run: `docker compose exec -T api pytest -q`
Expected: PASS (88 previos + 9 = 97).

- [ ] **Step 11: Commit**

```bash
git add backend/app/models/venta.py backend/app/schemas/venta.py \
  backend/app/services/venta_service.py backend/app/api/v1/ventas.py \
  backend/app/api/v1/router.py backend/tests/conftest.py \
  backend/tests/test_ventas_api.py
git commit -m "feat(api): cobro de pedido (venta, pagos, IVA, cambio, ticket)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 3: Pendientes de cobro — `GET /pedidos?por_cobrar=true`

**Files:**
- Modify: `backend/app/services/venta_service.py` (`listar_por_cobrar`)
- Modify: `backend/app/api/v1/pedidos.py` (param `por_cobrar`)
- Test: `backend/tests/test_ventas_api.py` (añadir un test)

**Interfaces:**
- Consumes: `Pedido`, `EstadoPedido`, `Venta` de `app.models`; `POST /ventas` (Task 2);
  `POST /pedidos/{id}/cancelar`; helpers `_pedido`, `_metodo_id` de `test_ventas_api.py`.
- Produces: `venta_service.listar_por_cobrar(db: Session) -> list[Pedido]`; el listado
  `GET /pedidos?por_cobrar=true`.

- [ ] **Step 1: Escribir el test (falla)**

Añadir al final de `backend/tests/test_ventas_api.py`:

```python
def test_pedidos_por_cobrar(client, db, admin_headers, cajero_headers):
    p_activo = _pedido(client, db, admin_headers, numero=610)

    p_cobrado = _pedido(client, db, admin_headers, numero=611, precio=116.0)
    ef = _metodo_id(db, "Efectivo")
    client.post(
        "/api/v1/ventas",
        headers=cajero_headers,
        json={
            "id_pedido": p_cobrado["id_pedido"],
            "pagos": [{"id_metodo_pago": ef, "monto": 200.0}],
        },
    )

    p_cancel = _pedido(client, db, admin_headers, numero=612)
    client.post(
        f"/api/v1/pedidos/{p_cancel['id_pedido']}/cancelar",
        headers=admin_headers,
        json={"motivo": "prueba"},
    )

    r = client.get("/api/v1/pedidos?por_cobrar=true", headers=cajero_headers)
    assert r.status_code == 200
    ids = {p["id_pedido"] for p in r.json()}
    assert p_activo["id_pedido"] in ids
    assert p_cobrado["id_pedido"] not in ids
    assert p_cancel["id_pedido"] not in ids
```

- [ ] **Step 2: Ejecutar para verificar que falla**

Run: `docker compose exec -T api pytest tests/test_ventas_api.py::test_pedidos_por_cobrar -v`
Expected: FAIL (el pedido cobrado y el cancelado aún aparecen — el filtro no existe).

- [ ] **Step 3: Añadir `listar_por_cobrar` al service**

En `backend/app/services/venta_service.py`, actualizar el import de modelos para incluir
`EstadoPedido`:

```python
from app.models import (
    Configuracion,
    EstadoPedido,
    MetodoPago,
    Pago,
    Pedido,
    Ticket,
    Venta,
)
```

Y añadir al final del archivo:

```python
def listar_por_cobrar(db: Session) -> list[Pedido]:
    cancelado = db.execute(
        select(EstadoPedido.id_estado).where(
            EstadoPedido.nombre_estado == "Cancelado"
        )
    ).scalar_one()
    con_venta = select(Venta.id_pedido)
    stmt = (
        select(Pedido)
        .where(Pedido.id_estado != cancelado, Pedido.id_pedido.not_in(con_venta))
        .order_by(Pedido.id_pedido.desc())
    )
    return list(db.execute(stmt).scalars())
```

- [ ] **Step 4: Añadir el parámetro `por_cobrar` al listado de pedidos**

En `backend/app/api/v1/pedidos.py`, añadir el import del service de ventas:

```python
from app.services import pedido_service, venta_service
```

(Si el import actual es `from app.services import pedido_service`, reemplázalo por la
línea de arriba.)

Y reemplazar el endpoint `listar` por:

```python
@router.get("", response_model=list[PedidoOut])
def listar(
    id_estado: int | None = None,
    estados: str | None = None,
    mias: bool = False,
    por_cobrar: bool = False,
    db: Session = Depends(get_db),
    current: Usuario = Depends(deps.get_current_user),
):
    if por_cobrar:
        return venta_service.listar_por_cobrar(db)
    id_usuario = current.id_usuario if mias else None
    if estados:
        ids = [int(x) for x in estados.split(",") if x.strip()]
    elif id_estado is not None:
        ids = [id_estado]
    else:
        ids = None
    return pedido_service.list_pedidos(db, ids, id_usuario)
```

- [ ] **Step 5: Ejecutar el test de por_cobrar (pasa)**

Run: `docker compose exec -T api pytest tests/test_ventas_api.py::test_pedidos_por_cobrar -v`
Expected: PASS.

- [ ] **Step 6: Suite backend completa (sin regresiones)**

Run: `docker compose exec -T api pytest -q`
Expected: PASS (97 previos + 1 = 98).

- [ ] **Step 7: Commit**

```bash
git add backend/app/services/venta_service.py backend/app/api/v1/pedidos.py \
  backend/tests/test_ventas_api.py
git commit -m "feat(api): listado de pedidos pendientes de cobro

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Notas de verificación final (tras Task 3)

- `docker compose exec -T api pytest -q` en verde (98 tests).
- Revisar en Swagger (`localhost:8000/docs`) que aparecen `POST /ventas`, `GET /ventas/{id}`
  y el parámetro `por_cobrar` en `GET /pedidos`.
- `progress.md` se actualiza al cerrar el Sprint 4 (no en este plan).
```
